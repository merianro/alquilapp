<% @user = User.find(params[:format])%>

<%= simple_form_for(@user, url: users_actualizar_contrasena_url) do |f| %> 
   <%= f.input :id, :as=> :hidden, input_html: {:value => current_user.id}%>
  <%= f.input :password, required: true, autofocus: true, input_html: { autocomplete: "password" } %>
  <%= f.input :password_confirmation, required: true, autofocus: true, input_html: { autocomplete: "password_confirmation" } %>
  <%= f.button :submit, 'Guardar cambios' %>
<% end %>