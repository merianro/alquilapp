<!DOCTYPE html>
<html>

<head>
  <title>Alquilapp</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  
  <!-- Boxicon -->
  <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
  <!--  -->
  
  <!-- Bootstrap 5.2 -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"></script>
  <!--  -->

  <!-- Mapkick -->
  <link href="https://api.tiles.mapbox.com/mapbox-gl-js/v0.45.0/mapbox-gl.css" rel="stylesheet" />
  <script src="https://api.tiles.mapbox.com/mapbox-gl-js/v0.45.0/mapbox-gl.js"></script>
  <script>
    mapboxgl.accessToken = "<%= Rails.application.credentials.mapbox %>";
  </script>
  <script src="https://unpkg.com/mapkick@0.1.1/dist/mapkick.js"></script>
  <!--  -->

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application" ,"data-turbo-track":"reload"%>
  <%= javascript_importmap_tags %>
</head>

<body id="body">
  <% if !user_signed_in? %>
    <%= render "application/header" %>
      <% elsif current_user.role=="driver" %>
        <%= render "application/left_sidebar_driver" %>
          <% elsif current_user.role=="su" %>
            <%= render "application/left_sidebar_su" %>
              <% elsif current_user.role=="admin" %>
                <%= render "application/left_sidebar_admin" %>
                  <% else %>
                    <% end %>
                      <%= yield %>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>

</body>

</html>