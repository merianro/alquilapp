<%= form_with(model: mp_webhook) do |form| %>
  <% if mp_webhook.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mp_webhook.errors.count, "error") %> prohibited this mp_webhook from being saved:</h2>

      <ul>
        <% mp_webhook.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
